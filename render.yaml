services:
  - type: web
    name: cloudterm
    env: node
    buildCommand: npm install && npm rebuild
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
